<?xml version="1.0"?>
<!-- Modified by BW-Tech GmbH for owncloud.online (PHP 8.4). -->
<info>
	<id>user_ldap</id>
	<name>LDAP Integration</name>
	<summary>Integrate LDAP user directories</summary>
	<description>
Looking to leverage your LDAP-based user directory? owncloud.online perfectly integrates with existing infrastructure making professional user management a breeze. With centrally managed directories users can just use their account credentials for owncloud.online as with any other service that is provided. Simultaneously IT is relieved as there is no need to care about different user accounts for specific services. Just connect owncloud.online to a user directory and you're good to go!

This application enables administrators to connect owncloud.online to an LDAP-based user directory for authenticating and provisioning users, groups and user attributes. Administrators can configure this application to connect to one or more LDAP directories or Active Directories via an LDAP interface. Attributes such as user quota, email, avatar pictures, group memberships and more can be pulled into owncloud.online from a directory with the appropriate queries and filters.

A user logs into owncloud.online with their LDAP/AD credentials, and is granted access based on an authentication request handled by the LDAP/AD server. owncloud.online does not store LDAP/AD passwords, rather these credentials are used to authenticate a user. owncloud.online then uses a session for the user ID. 
More information is available in the LDAP User and Group Backend documentation.

This is a PHP 8.4 fork maintained by BW-Tech GmbH for owncloud.online.
	</description>
	<licence>AGPL</licence>
	<author>Jörn Friedrich Dreyer, Tom Needham, Juan Pablo Villafañez Ramos, Dominik Schmidt and Arthur Schiwon, modified by BW-Tech GmbH</author>
	<version>0.20.3</version>
	<types>
		<authentication/>
	</types>
	<documentation>
		<admin>https://github.com/BWTECH-github/user_ldap#readme</admin>
	</documentation>
	<category>integration</category>
	<dependencies>
		<lib>ldap</lib>
		<owncloud min-version="11" max-version="11" />
		<php min-version="8.4" />
	</dependencies>

	<namespace>User_LDAP</namespace>

	<settings>
		<admin>OCA\User_LDAP\AdminPanel</admin>
	</settings>

	<commands>
		<command>OCA\User_LDAP\Command\ShowConfig</command>
		<command>OCA\User_LDAP\Command\SetConfig</command>
		<command>OCA\User_LDAP\Command\TestConfig</command>
		<command>OCA\User_LDAP\Command\CreateEmptyConfig</command>
		<command>OCA\User_LDAP\Command\DeleteConfig</command>
		<command>OCA\User_LDAP\Command\Search</command>
		<command>OCA\User_LDAP\Command\CheckUser</command>
		<command>OCA\User_LDAP\Command\InvalidateCache</command>
	</commands>

	<use-migrations>true</use-migrations>
</info>
